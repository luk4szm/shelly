{% extends 'base.html.twig' %}

{% block pageTitle %}Location{% endblock %}

{% block pageTitleActions %}Bufor{% endblock %}

{% block pageBody %}
    <div class="container-xl">
        <div class="row row-deck row-cards">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        {{ dump(temperature_15m) }}
                        {{ dump(temperature_05m) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
